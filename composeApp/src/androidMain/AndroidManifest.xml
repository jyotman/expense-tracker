<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required on Android 11+ so getInstalledApplications() returns all user-installed apps
         for the capture-apps picker. Without this, only apps the system grants visibility to
         (typically just the app itself) are returned. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application>

        <!-- Reads bank/wallet/card notifications to auto-create expenses. The user must
             explicitly grant "Notification access" in system settings. -->
        <service
            android:name="app.expensetracker.capture.SpendNotificationListener"
            android:label="@string/notification_listener_label"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Home-screen widget: current month spend + quick add. -->
        <receiver
            android:name="app.expensetracker.widget.ExpenseTrackerWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/expense_tracker_widget_info" />
        </receiver>

    </application>

</manifest>
